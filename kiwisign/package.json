{
  "name": "kiwisign",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ kiwisign",
    "create-local": "graph create --node http://localhost:8020/ kiwisign",
    "remove-local": "graph remove --node http://localhost:8020/ kiwisign",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kiwisign"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.2",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
